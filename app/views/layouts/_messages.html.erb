<%if !!flash[:error] || !!flash[:success] %>
<div class="container">
  <div class="row signup-form">
    <div class="col-s12 m12 l12">
      <div class="card-panel red lighten-1" id="fadeout">
        <% flash.each do |name,msg| %>
          <li><%= msg %></li>
        <%end%>
      </div>
    </div>
  </div>
</div>
<%end %>